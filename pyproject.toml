[project]
name = "menuyukti-workspace"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
  "menuyukti",
  "graphql",
  "agents-app",
  "agent-skills",
]

[tool.uv.workspace]
members = [
  "packages/menuyukti",
  "packages/agent-skills",
  "apps/graphql",
  "apps/agents",
]

[tool.uv.sources]
menuyukti = { workspace = true }
graphql = { workspace = true }
agents-app = { workspace = true }
agent-skills = { workspace = true }

[dependency-groups]
dev = [
  "ipykernel>=7.1.0",
  "matplotlib>=3.10.8",
  "openpyxl>=3.1.5",
  "pandas>=2.3.3",
  "psycopg[binary]>=3.2.13",
  "pytest>=9.0.0",
  "python-dotenv>=1.2.1",
  "sqlalchemy>=2.0.44",
]

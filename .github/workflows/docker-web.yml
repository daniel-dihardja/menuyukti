# Build and push web image (menuyukti-web). Manual only — Actions → Run workflow (optional tag).
# After changing this workflow or Clerk build-args: push to default branch, add repo secrets if missing,
#   run this workflow, then run "Docker — remote compose pull" to deploy.
# Server: run "Docker — remote compose pull" after images exist on Docker Hub.
#
# Secrets: DOCKERHUB_USERNAME, DOCKERHUB_PASSWORD, DATABASE_URL, NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY,
#   CLERK_SECRET_KEY (see docker-build-push.reusable.yml)
# Optional variable: DOCKER_IMAGE_PREFIX (default johngoyason)

name: Docker — web

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Optional single tag (empty = sha-* + latest on default branch)"
        required: false
        type: string
        default: ""

jobs:
  build-push:
    uses: ./.github/workflows/docker-build-push.reusable.yml
    secrets: inherit
    with:
      service: web
      dockerfile: apps/web/Dockerfile
      image_repository: ${{ format('{0}/menuyukti-web', vars.DOCKER_IMAGE_PREFIX != '' && vars.DOCKER_IMAGE_PREFIX || 'johngoyason') }}
      custom_tag: ${{ github.event.inputs.tag }}

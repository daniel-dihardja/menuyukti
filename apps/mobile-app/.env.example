# Base URL of the GraphQL/CRM API (no trailing slash).
# Simulator / local: http://localhost:8000
# Physical device on LAN (dev only): http://<your-lan-ip>:8000
# Production / staging: use HTTPS (e.g. https://api.example.com)
EXPO_PUBLIC_CRM_API_URL=http://localhost:8000

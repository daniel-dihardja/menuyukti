# OpenAI (required for chat)
OPENAI_API_KEY=

# Optional: override default model (see agents_app.models.llm_config)
# OPENAI_MODEL=gpt-4o-mini

# GraphQL (required for milestone run — same endpoint/key as apps/web)
GRAPHQL_ENDPOINT=
# GRAPHQL_INTERNAL_API_KEY=
